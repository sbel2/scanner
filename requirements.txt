# Core dependencies
anthropic>=0.40.0        # LLM backend (requires ANTHROPIC_API_KEY in .env)
resend>=2.0.0            # Email delivery
httpx>=0.27.0            # HTTP client for calendar/feed fetching
tavily-python>=0.5.0     # Web search (long-tail discovery)
pydantic>=2.5.0          # Typed data models
jinja2>=3.1.0            # Email template rendering
python-dotenv>=1.0.0     # .env file loading
pyyaml>=6.0.0            # mission.yaml parsing
feedparser>=6.0.11       # RSS feed parsing

# Optional: only needed if using claude-agent-sdk (no ANTHROPIC_API_KEY)
# claude-agent-sdk>=0.1.0
